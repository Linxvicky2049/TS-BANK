import {NavLink,Outlet,useNavigate} from 'react-router-dom'; import {Home,CreditCard,ShieldCheck,ArrowLeftRight,Search,FileClock,LogOut} from 'lucide-react'; import {useAuth} from '../context/AuthContext';
const links=[['/dashboard','Dashboard',Home],['/account','My Account',CreditCard],['/onboarding','KYC Verification',ShieldCheck],['/transfer','Transfer',ArrowLeftRight],['/name-enquiry','Name Enquiry',Search],['/transactions','Transactions',FileClock]] as const;
export default function Layout(){const {user,logout}=useAuth();const nav=useNavigate();return <div className="shell"><aside><div className="logo"><b>TS</b><div><strong>TS BANK</strong><small>Digital Banking</small></div></div><nav>{links.map(([to,l,I])=><NavLink key={to} to={to} className={({isActive})=>isActive?'active':''}><I size={17}/>{l}</NavLink>)}</nav><div className="side-bottom"><div className="customer">{user?.fullName||'Customer'}<small>{user?.email||''}</small></div><button onClick={()=>{logout();nav('/login')}}><LogOut size={17}/>Sign out</button></div></aside><main><header><div><small>TS BANKING PLATFORM</small><h1>Secure. Simple. Yours.</h1></div><span className="secure">● Secure session</span></header><section><Outlet/></section></main></div>}
